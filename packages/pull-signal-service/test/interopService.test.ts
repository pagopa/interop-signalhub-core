import { describe, expect, it } from "vitest";

describe.skip("PDND Interoperability service", () => {
  it("should give permission to a consumer for ", async () => {
    expect(1).toBe(1);
  });
  it("should deny permission to a consumer for ", async () => {
    expect(1).toBe(1);
  });
});
